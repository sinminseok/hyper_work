<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender (개발/로컬용) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- CloudWatch Logs Appender - Error Log -->
    <appender name="CLOUDWATCH_ERROR" class="ca.pjer.logback.AwsLogsAppender">
        <awsAccessKey>${AWS_S3_ACCESS_KEY}</awsAccessKey>
        <awsSecretKey>${AWS_S3_SECRET_KEY}</awsSecretKey>
        <logGroupName>/funnyrun/error</logGroupName>
        <logStreamName>error-%d{yyyy-MM-dd}</logStreamName>
        <logRegion>ap-northeast-2</logRegion>
        <maxBatchLogEvents>50</maxBatchLogEvents>
        <maxFlushTimeMillis>30000</maxFlushTimeMillis>
        <maxBlockTimeMillis>5000</maxBlockTimeMillis>
        <retentionTimeDays>7</retentionTimeDays>
        <layout>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </layout>
    </appender>

    <!-- WebSocket 통계 로그 비활성화 (모든 환경) -->
    <logger name="org.springframework.web.socket.config.WebSocketMessageBrokerStats" level="OFF"/>

    <!-- 개발 환경 (dev, test) - Console만 사용 -->
    <springProfile name="dev,test">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>

    <!-- 운영 환경 (prod) - ERROR 레벨만 출력 + CloudWatch -->
    <springProfile name="prod">
        <root level="ERROR">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="CLOUDWATCH_ERROR" />
        </root>
    </springProfile>
</configuration>
